/*
#include <iostream>

using namespace std;

int main()
{
	int n1, n2, tmp1, tmp2, a, b, c;
	
	cin >> n1 >> n2;

	a = n1 / 100;
	b = (n1 - (100 * a)) / 10;
	c = n1 - (100 * a + 10 * b);

	tmp1 = 100 * c + 10 * b + a;
	
	a = n2 / 100;
	b = (n2 - (100 * a)) / 10;
	c = n2 - (100 * a + 10 * b);

	tmp2 = 100 * c + 10 * b + a;

	if (tmp1 >= tmp2) {
		cout << tmp1 << endl;
	}
	else {
		cout << tmp2 << endl;
	}

	return 0;
}
*/