/*
#include <cstdio>
#include <algorithm>
using namespace std;
int n, k, N[5000000];
int main()
{
	scanf("%d %d", &n, &k);
	for (int i = 0; i < n; i++) scanf("%d", N + i);
	sort(N, N + n);
	printf("%d\n", N[k-1]);
	return 0;
}
*/