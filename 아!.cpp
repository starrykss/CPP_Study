/*
#include <iostream>
#include <cstring>	// strlen()

#define N 999

using namespace std;

int main()
{
	int len, cmplen;
	char ary[N], cmp[N];

	cin >> ary >> cmp;
	
	len = strlen(ary);
	cmplen = strlen(cmp);

	if (len >= cmplen) {
		cout << "go" << endl;
	}
	else {
		cout << "no" << endl;
	}

	return 0;
}
*/